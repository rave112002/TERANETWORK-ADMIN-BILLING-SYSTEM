# Base URL of the backend API. The Vite dev server proxies nothing — the app
# calls this origin directly, so the backend CORS must allow the Vite origin
# (http://localhost:5173), which it does.
VITE_BASE_URL=http://localhost:3000
